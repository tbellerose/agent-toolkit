// SET_USER
export const setUser = (user) => ({
  type: 'SET_USER',
  user
});

// CLEAR_USER
export const clearUser = () => ({
  type: 'CLEAR_USER'
});

// SET_TOKEN
export const setToken = (token) => ({
  type: 'SET_TOKEN',
  token
});

// CLEAR_TOKEN
export const clearToken = () => ({
  type: 'CLEAR_TOKEN'
});